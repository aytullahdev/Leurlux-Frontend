import Property from '@/components/property/Property';
import React from 'react';

const page = () => {
    return (
        <div>
            <Property />
        </div>
    );
};

export default page;